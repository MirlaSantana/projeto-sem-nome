<nav class="navbar navbar-inverse" role="navigation">
  <div class="navbar-header">
    <a class="navbar-brand" href="#">Habilidades</a>
  </div>

  <div class="collapse navbar-collapse navbar-ex1-collapse">
    <ul class="nav navbar-nav">
      <li><%= link_to 'Novo', new_theme_path(params[:project_id]) %></li>
    </ul>
  </div>
</nav>

<table class="table table-hover table-bordered">
  <thead>
    <tr>
      <th>Nome</th>
    </tr>
  </thead>
  <tbody>
    <% @themes.each do |theme| %>
    <tr>
      <td><%= theme.name %></td>
      <td>
        <%= link_to edit_theme_path(theme.id), title: 'Editar' do  %>
          <span class="glyphicon glyphicon-pencil"></span>
        <% end %>
        <%= link_to show_theme_path(theme.id), title: 'Exibir' do  %>
          <span class="glyphicon glyphicon-eye-open"></span>
        <% end %>
        <%= link_to destroy_theme_path(theme.id), title: 'Excluir' do %>
          <span class="glyphicon glyphicon-trash"></span>
        <% end %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
