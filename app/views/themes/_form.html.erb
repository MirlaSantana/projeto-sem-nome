<div class="container">
	<div class="col-md-6">
		<%= form_for @theme, url: @path_to_save, method: 'post', html: {id: 'form-themes'} do |t| %>
		  <div class="form-group">
		  	<%= t.label :name %>
		    <%= t.text_field :name, class: 'form-control' %>
		  </div>
		  <div>
		  	<%= t.submit 'Salvar', class: 'btn btn-success' %>
		  </div>
		<% end %>
	</div>
</div>

<script type="text/javascript">
	// $("#salvar").click(function(){
	// 	$("#form-ability").submit();
	// });
</script>
